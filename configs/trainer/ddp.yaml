defaults:
  - default

strategy:
  _target_: lightning.pytorch.strategies.ddp.DDPStrategy
  find_unused_parameters: true

accelerator: gpu
devices: 4
num_nodes: 1
sync_batchnorm: true
max_epochs: 250
